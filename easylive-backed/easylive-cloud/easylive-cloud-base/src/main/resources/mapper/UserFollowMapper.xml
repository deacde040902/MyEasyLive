<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easylive.base.mapper.UserFollowMapper">
    
    <!-- 查询用户关注列表 -->
    <select id="selectFollowList" resultType="com.easylive.base.entity.po.UserFollow">
        SELECT * FROM user_follow 
        WHERE follower_id = #{followerId} 
        AND status = 0 
        ORDER BY create_time DESC 
        LIMIT #{size} OFFSET #{page}
    </select>
    
    <!-- 查询用户粉丝列表 -->
    <select id="selectFanList" resultType="com.easylive.base.entity.po.UserFollow">
        SELECT * FROM user_follow 
        WHERE following_id = #{followingId} 
        AND status = 0 
        ORDER BY create_time DESC 
        LIMIT #{size} OFFSET #{page}
    </select>
    
    <!-- 检查是否已关注 -->
    <select id="selectByFollowerAndFollowing" resultType="com.easylive.base.entity.po.UserFollow">
        SELECT * FROM user_follow 
        WHERE follower_id = #{followerId} 
        AND following_id = #{followingId}
    </select>
</mapper>